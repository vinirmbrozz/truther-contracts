// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.1
//   protoc               v3.21.12
// source: transaction.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "truther.transaction";

export interface PredictiveAnalyzer {
  isAllowed: boolean;
  reason: string;
  cardId: string;
  userId: string;
  walletAddress: string;
  allowance: string;
}

export interface Transaction {
  transactionAmount: string;
  predictiveAnalyzer: PredictiveAnalyzer | undefined;
  finalDecision: string;
}

function createBasePredictiveAnalyzer(): PredictiveAnalyzer {
  return { isAllowed: false, reason: "", cardId: "", userId: "", walletAddress: "", allowance: "" };
}

export const PredictiveAnalyzer: MessageFns<PredictiveAnalyzer> = {
  encode(message: PredictiveAnalyzer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.isAllowed !== false) {
      writer.uint32(8).bool(message.isAllowed);
    }
    if (message.reason !== "") {
      writer.uint32(18).string(message.reason);
    }
    if (message.cardId !== "") {
      writer.uint32(26).string(message.cardId);
    }
    if (message.userId !== "") {
      writer.uint32(34).string(message.userId);
    }
    if (message.walletAddress !== "") {
      writer.uint32(7638874).string(message.walletAddress);
    }
    if (message.allowance !== "") {
      writer.uint32(50).string(message.allowance);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PredictiveAnalyzer {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePredictiveAnalyzer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.isAllowed = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.cardId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 954859: {
          if (tag !== 7638874) {
            break;
          }

          message.walletAddress = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.allowance = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PredictiveAnalyzer {
    return {
      isAllowed: isSet(object.isAllowed) ? globalThis.Boolean(object.isAllowed) : false,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      cardId: isSet(object.cardId) ? globalThis.String(object.cardId) : "",
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      walletAddress: isSet(object.walletAddress) ? globalThis.String(object.walletAddress) : "",
      allowance: isSet(object.allowance) ? globalThis.String(object.allowance) : "",
    };
  },

  toJSON(message: PredictiveAnalyzer): unknown {
    const obj: any = {};
    if (message.isAllowed !== false) {
      obj.isAllowed = message.isAllowed;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.cardId !== "") {
      obj.cardId = message.cardId;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.walletAddress !== "") {
      obj.walletAddress = message.walletAddress;
    }
    if (message.allowance !== "") {
      obj.allowance = message.allowance;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PredictiveAnalyzer>, I>>(base?: I): PredictiveAnalyzer {
    return PredictiveAnalyzer.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PredictiveAnalyzer>, I>>(object: I): PredictiveAnalyzer {
    const message = createBasePredictiveAnalyzer();
    message.isAllowed = object.isAllowed ?? false;
    message.reason = object.reason ?? "";
    message.cardId = object.cardId ?? "";
    message.userId = object.userId ?? "";
    message.walletAddress = object.walletAddress ?? "";
    message.allowance = object.allowance ?? "";
    return message;
  },
};

function createBaseTransaction(): Transaction {
  return { transactionAmount: "", predictiveAnalyzer: undefined, finalDecision: "" };
}

export const Transaction: MessageFns<Transaction> = {
  encode(message: Transaction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.transactionAmount !== "") {
      writer.uint32(10).string(message.transactionAmount);
    }
    if (message.predictiveAnalyzer !== undefined) {
      PredictiveAnalyzer.encode(message.predictiveAnalyzer, writer.uint32(18).fork()).join();
    }
    if (message.finalDecision !== "") {
      writer.uint32(26).string(message.finalDecision);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Transaction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTransaction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.transactionAmount = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.predictiveAnalyzer = PredictiveAnalyzer.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.finalDecision = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Transaction {
    return {
      transactionAmount: isSet(object.transactionAmount) ? globalThis.String(object.transactionAmount) : "",
      predictiveAnalyzer: isSet(object.predictiveAnalyzer)
        ? PredictiveAnalyzer.fromJSON(object.predictiveAnalyzer)
        : undefined,
      finalDecision: isSet(object.finalDecision) ? globalThis.String(object.finalDecision) : "",
    };
  },

  toJSON(message: Transaction): unknown {
    const obj: any = {};
    if (message.transactionAmount !== "") {
      obj.transactionAmount = message.transactionAmount;
    }
    if (message.predictiveAnalyzer !== undefined) {
      obj.predictiveAnalyzer = PredictiveAnalyzer.toJSON(message.predictiveAnalyzer);
    }
    if (message.finalDecision !== "") {
      obj.finalDecision = message.finalDecision;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Transaction>, I>>(base?: I): Transaction {
    return Transaction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Transaction>, I>>(object: I): Transaction {
    const message = createBaseTransaction();
    message.transactionAmount = object.transactionAmount ?? "";
    message.predictiveAnalyzer = (object.predictiveAnalyzer !== undefined && object.predictiveAnalyzer !== null)
      ? PredictiveAnalyzer.fromPartial(object.predictiveAnalyzer)
      : undefined;
    message.finalDecision = object.finalDecision ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
